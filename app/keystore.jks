signingConfigs {
    create("release") {
        storeFile = file("keystore.jks")
        storePassword = "161105"
        keyAlias = "cloudplay_new"
        keyPassword = "161105"
        storeType = "PKCS12"
    }
}
