signingConfigs {
    create("release") {
        storeFile = file("keystore.jks")
        storePassword = "161105"
        keyAlias = "cloudplay"
        keyPassword = "161105"
        storeType = "PKCS12"
    }
}
